// Phase 3: customer accounts, login, order history, and the admin
// dashboard side of this all live here once the backend exists. Empty
// stub for now, same pattern as /menu and /cart, so the header's
// account icon has somewhere real to go.
export default function AccountPage() {
  return (
    <main className="flex min-h-screen items-center justify-center bg-cream px-6 pt-24 text-center">
      <p className="font-display text-heading text-berry">
        Accounts are coming soon.
      </p>
    </main>
  );
}
